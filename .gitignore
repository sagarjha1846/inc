# The ledger of issued license keys — customer emails and key ids.
# Never commit this.
licenses.ndjson

# Local signing secret, if you keep one in a file rather than the environment.
.env
.env.*
license.private.json

# wrangler dev reads this for local secrets — it holds LICENSE_SECRET.
.dev.vars
.dev.vars.*

node_modules/
.wrangler/
dist/
*.log
.DS_Store

# Static site build output (published by .github/workflows/pages.yml)
site/
